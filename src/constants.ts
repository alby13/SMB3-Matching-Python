import { CardType } from './domain/CardType'

// These should match the animation values in index.css, where applicable
export const FLIP_DURATION = 256
export const OUTCOME_SOUND_DELAY = 384
export const OUTCOME_AFTER_SOUND_PLAYED = OUTCOME_SOUND_DELAY + 150
export const OUTCOME_FLIP_BACK_DELAY = 1280
export const ENV_REVEAL_MUSIC_DELAY = 512
